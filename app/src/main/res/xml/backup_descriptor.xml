<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <exclude
        domain="database"
        path="Event_Manager_Database" />
    <exclude
        domain="database"
        path="ScheduleDatabase" />
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude
        domain="sharedpref"
        path="MyPrefsFile1.xml" />
    <exclude
        domain="sharedpref"
        path="Cart details.xml" />
</full-backup-content>
